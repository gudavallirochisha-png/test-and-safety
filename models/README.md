# Machine Learning Models Directory (`models/`)

Stores trained model weights and artifacts:
- `xgboost_risk_v1.json` (Structured tabular weights)
- `distilbert_review_v1/` (HuggingFace transformers checkpoints)
- `yolo_authenticity_v1.pt` (Ultralytics PyTorch weights)
